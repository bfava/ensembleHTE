% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.check_small_cells}
\alias{.check_small_cells}
\title{Check for small fold (or fold x restrict_by) cells and warn}
\usage{
.check_small_cells(fold_list, n_groups, restrict_by = NULL, func_name = "")
}
\arguments{
\item{fold_list}{A list of integer vectors of fold assignments (one per
repetition), or a single integer vector. When a list is provided, all
repetitions are scanned and the worst case is reported.}

\item{n_groups}{Number of groups requested}

\item{restrict_by}{Optional factor/integer vector for restricted ranking
(same length as each fold vector). Assumed constant across repetitions
(only fold assignments change).}

\item{func_name}{Character name of the calling function (for the warning
message)}
}
\value{
Invisible NULL. Emits a warning if any cell is too small.
}
\description{
Checks whether any fold (or fold x restrict_by) cell has fewer observations than
the requested number of groups across all repetitions. When this happens,
some groups will be empty in those cells and observations are assigned
deterministically to the lowest groups. This is not an error, but users
should be aware of it.
}
\keyword{internal}
