% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_hte.R
\name{.clan_single}
\alias{.clan_single}
\title{Internal function to compute CLAN for a single repetition}
\usage{
.clan_single(
  predicted_values,
  fold,
  n_groups,
  variables,
  variable_data,
  na_rm = FALSE,
  group_ref_idx = NULL,
  analysis_idx = NULL,
  cluster_id = NULL
)
}
\arguments{
\item{predicted_values}{Predicted values (ITE or Y) vector for this repetition (full length)}

\item{fold}{Fold assignment vector for this repetition (full length)}

\item{n_groups}{Number of groups for CLAN}

\item{variables}{Character vector of variable names to analyze}

\item{variable_data}{data.table/data.frame with the variables to analyze (full length)}

\item{na_rm}{Logical, whether to remove NA values in calculations (default: FALSE)}

\item{group_ref_idx}{Optional logical vector (same length as predicted_values).
When provided, group cutoffs are computed from only the reference observations,
then applied to all observations. If \code{NULL}, all observations are used.}

\item{analysis_idx}{Optional logical vector (same length as predicted_values).
When provided, means and SEs are computed only for these observations (after
group assignment). If \code{NULL}, all observations are used.}

\item{cluster_id}{Optional vector of cluster identifiers for cluster-robust SEs.
When provided, uses a regression-based approach with clustered inference
instead of the default analytical SE formulas.}
}
\value{
data.table with CLAN estimates for each variable
}
\description{
Internal function to compute CLAN for a single repetition
}
\keyword{internal}
