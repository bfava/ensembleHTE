% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_hte.R
\name{.gates_single}
\alias{.gates_single}
\title{Internal function to compute GATES for a single repetition}
\usage{
.gates_single(
  Y,
  D,
  prop_score,
  weight,
  predicted_values,
  fold,
  n_groups = 5,
  restrict_by = NULL,
  controls = NULL,
  control_data = NULL,
  group_ref_idx = NULL,
  analysis_idx = NULL,
  cluster_id = NULL
)
}
\arguments{
\item{Y}{Outcome vector (full length)}

\item{D}{Treatment vector (full length)}

\item{prop_score}{Propensity score vector (full length)}

\item{weight}{Weight vector (full length)}

\item{predicted_values}{Predicted values (ITE or Y) vector for this repetition (full length)}

\item{fold}{Fold assignment vector for this repetition (full length)}

\item{n_groups}{Number of groups for GATES}

\item{restrict_by}{Optional restrict_by indicator for restricted ranking (factor/integer vector, full length)}

\item{controls}{Optional control variables (character vector of column names)}

\item{control_data}{Optional data.table/data.frame with control variables (full length)}

\item{group_ref_idx}{Optional logical vector (same length as Y). When provided,
group cutoffs are computed from only the reference observations, then applied
to all observations. If \code{NULL}, all observations are used for group formation.}

\item{analysis_idx}{Optional logical vector (same length as Y). When provided,
the regression is run only on these observations (after group assignment).
If \code{NULL}, all observations are used for the regression.}

\item{cluster_id}{Optional vector of cluster identifiers for cluster-robust SEs.
When provided, passed to \code{reg_from_formula} for clustered inference.}
}
\value{
List with group_estimates, tests (top_bottom, all, top_all), and reg object
}
\description{
Internal function to compute GATES for a single repetition
}
\keyword{internal}
